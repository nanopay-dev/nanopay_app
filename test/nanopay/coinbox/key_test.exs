defmodule Nanopay.Coinbox.KeyTest do
  use Nanopay.DataCase

  describe "derive_pubkey/1" do
    @tag :pending
    test "returns a PubKey for the given coin"
  end

  describe "derive_script/1" do
    @tag :pending
    test "returns a Script for the given coin"
  end

  describe "derive_sig/5" do
    @tag :pending
    test "returns a Signature for the given coin and tx context"
  end

  describe "derive_sig_advanced/5" do
    @tag :pending
    test "returns a Signature for the given coin and tx context"
  end

end
